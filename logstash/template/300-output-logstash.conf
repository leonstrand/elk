output {
  if [type] != "heartbeat" {
    elasticsearch {
      hosts => REPLACE
      document_id => "%{[@metadata][fingerprint]}"
      max_open_files => 8190
      close_older => 172800
    }
  }
}
